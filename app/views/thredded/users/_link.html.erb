<% if user %>
  <% if !user.thredded_anonymous? %>
    <%= link_to user.to_s, profile_path(user.profile_id) %>
  <% else %>
    <%= user.to_s %>
  <% end %>
<% else %>
  <em><%= t 'thredded.null_user_name' %></em>
<% end %>
